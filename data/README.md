# Match Data

Run `bash setup.sh` to download StatsBomb open-data matches.
This requires `git` to be installed.

After setup:
- `data/matches/` — match event JSON files (one per match)
- `data/lineups/` — lineup JSON files (one per match)

Filenames are StatsBomb match IDs (e.g. `69249.json`).
